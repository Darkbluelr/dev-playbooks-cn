# specs/（规格目录约定）

本目录用于存放规格文件（真理源或变更包增量），按 capability 分目录组织。

## 路径映射（以配置发现为准）

- 真理源（Truth Root）：`dev-playbooks/specs/`
- 变更包（Change Root）：`dev-playbooks/changes/`

## 目录结构

- 真理源：`dev-playbooks/specs/<capability>/spec.md`
- 变更包增量：`dev-playbooks/changes/<change-id>/specs/<capability>/spec.md`
